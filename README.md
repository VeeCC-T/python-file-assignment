# 📝 Python File Handling Assignment

This repository contains my solutions for the **File Read & Write Challenge** and the **Error Handling Lab** in Python.

---

## 📂 Files in this repo
- `file_challenge.py` → Reads from `input.txt`, modifies the content, and writes to `output.txt`.
- `error_handling_lab.py` → Asks the user for a filename and displays the content safely (with error handling).
- `input.txt` → Sample input file used in Part 1.
- `output.txt` → Output file generated by Part 1.

---

## 🚀 How to Run
1. Make sure you have **Python 3** installed.  
   Check with:
   ```bash
   python --version
